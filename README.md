# API Lista de Compras

API em Python (Flask) para gerenciar lista de compras (adicionar, atualizar, deletar e visualizar).
 Como Executar
1. Instale as dependências:
   ```bash
   pip install -r requisitos.txt


   python main.py
